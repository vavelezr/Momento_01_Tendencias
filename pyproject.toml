[project]
name = "momento-01-tendencias"
version = "0.1.0"
description = "Momento 1 — CI/CD en Base de Datos (reseñas de negocios locales, Goodyear AZ)"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
    "snowflake-connector-python>=3.12",
    "cryptography>=42.0",
    "faker>=40.37.0",
]

[dependency-groups]
dev = [
    "ruff>=0.6",
]
